server:
    port : 9191
spring:
  profiles:
    active: prod
  datasource:
    url: jdbc:mariadb://${mariadb_address}:3306/codespace
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: codespace
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  jwt:
    secret: codeholiccodeholiccodeholiccodeholiccodeholiccodeholiccodeholic
  cache:
    type: redis
  redis:
    host: ${redis_address}
    port: 6379
openstackDomain: ${openstack_address}
countPerPage: 5